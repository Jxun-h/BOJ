#include <iostream>

using namespace std;

int main() {
    long n, m, k;
    cin >> n >> m >> k;

    cout << fixed;
    cout.precision(13);
    
    cout << (n + m + k) << endl;

    return 0;
}
