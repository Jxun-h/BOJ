#include <iostream>

using namespace std;

int main() {
    long t, x, y;
    
    cin >> t;
    
    for (int i = 0; i < t; i ++) {
        cin >> x >> y;
        cout << x + y << "\n";
    }
    
    return 0;
}
