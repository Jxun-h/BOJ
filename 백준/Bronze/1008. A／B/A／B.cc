#include <iostream>

using namespace std;

int main() {
    double n, m;
    cin >> n >> m;

    cout << fixed;
    cout.precision(9);
    
    cout << (n / m) << endl;

    return 0;
}
